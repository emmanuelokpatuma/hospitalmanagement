image:
  repository: omagu/appointment-api
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 3001
  type: ClusterIP

env:
  DB_SERVER: hospital-ui-hpm-sqlserver

sqlserver:
  enabled: false

replicaCount: 1

ingress:
  enabled: false
  hosts:
    - host: hospital.local
      paths:
        - path: /appointment
          pathType: Prefix

serviceAccount:
  create: true
  name: "hospital-appointment-sa"

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationPercentage: 80
